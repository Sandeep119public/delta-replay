# Delta Replay API

FastAPI service boundary for replay and trading logic.

```bash
python -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
python -m uvicorn app.main:app --reload
```
